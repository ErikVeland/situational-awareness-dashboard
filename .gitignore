node_modules
dist
.DS_Store
coverage
*.log
.env
.env.*
!.env.example

# Assignment brief is confidential — keep out of source control
*.pdf

# Local agent/tool state
.claude/
.playwright-mcp/

# TypeScript build-info cache (emitted by `tsc -b`, not source)
*.tsbuildinfo

# Local screenshots (kept out of source control)
dark-mode-*.png
light-mode-*.png
light-fresh-load.png
